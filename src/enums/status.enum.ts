export enum CourseStatus {
  DRAFT = "draft",
  PUBLISHED = "published",
  ARCHIVED = "archived",
}

export enum EnrollmentStatus {
  PENDING = "pending",
  ACTIVE = "active",
  COMPLETED = "completed",
  CANCELLED = "cancelled",
}

export enum AssignmentStatus {
  PENDING = "pending",
  SUBMITTED = "submitted",
  GRADED = "graded",
  LATE = "late",
}
